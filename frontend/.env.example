# DocQ&A React UI environment
# Copy this to .env and adjust the value if your FastAPI backend is not on localhost:8000.
# With vite.config.ts proxy enabled, you can also leave this empty and the proxy handles routing.
VITE_API_BASE_URL=http://localhost:8000
